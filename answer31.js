const newUserList =[];
// userList[0]="Admin";
// userList[1]="Shiraz";
// userList[2]="Javed";
// userList[3]="Farooq";
// userList[4]="Maaz";

const checkTheList = newUserList.length

    if (checkTheList>=1)
{
    console.log ("List contains %s users in it.",checkTheList);
  
}
else 
{
    console.log("The List is empty, we need to find some users.");
}
